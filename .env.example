# Backend API (server.ts) — real auth via SQLite + bcrypt + JWT, no external service required
JWT_SECRET=generate-with-openssl-rand-hex-32
CORS_ORIGIN=http://localhost:3001

# Supabase Configuration (deferred — not currently used; authService.ts talks to server.ts instead)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Google Gemini API
VITE_GEMINI_API_KEY=your-gemini-api-key-here

# Optional: Third-party integrations
VITE_STRIPE_PUBLIC_KEY=your-stripe-key
VITE_OPENAI_API_KEY=your-openai-key
VITE_ANTHROPIC_API_KEY=your-claude-key

# Sentry Configuration (Error Tracking + Performance Monitoring)
VITE_SENTRY_DSN=
VITE_SENTRY_ENABLED=true
VITE_SENTRY_ENVIRONMENT=development
VITE_SENTRY_TRACE_SAMPLE_RATE=0.1

# App Configuration
VITE_APP_NAME=Sacred Core
VITE_APP_VERSION=1.0.0
VITE_LOG_LEVEL=info
